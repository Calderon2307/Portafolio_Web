import style from './Title.module.css';

const Title = () => {

    return (
        <h2 className={`${style.title}`}>
            Otras personas tambien comentaron
        </h2>
    );
};

export default Title;